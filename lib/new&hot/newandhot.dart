import 'package:flutter/material.dart';

class NewandHotScreen extends StatelessWidget {
  const NewandHotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(child: Text("NEW HOT SCREEN"),),
    );
  }
}